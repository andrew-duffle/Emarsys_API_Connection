from setuptools import setup, find_packages
setup(
	name='emarsys_keys',
	version='1.0',
	author='andrew duffle',
	author_email='andrew.duffle@fuerstgroup.com',
	packages=find_packages(exclude=('tests','docs'))
)
